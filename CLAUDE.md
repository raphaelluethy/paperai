Use Bun as the runtime, package manager, bundler, and test runner.
Use `Bun.serve()` with HTML imports for the frontend (not vite/express).
Use Bun built-in APIs: `bun:sqlite`, `Bun.redis`, `Bun.sql`, `Bun.file`, `Bun.$`.
Always propose the simplest solution that satisfies the requirement.
Search the codebase for existing types before defining new ones.
For questions about `solidjs` or `effect`, use `btca ask -r [solidjs | effect] -q "question"`.
